# Тема урока: Триггеры в SQL 

`Triggers` нужны для того, чтобы давать реакцию на определенные события. 
В SQL синтаксис триггеров очень просто и создается на Insert, Update, Delete 

Также при создании триггеров важно писать время выполнения - after, before, instead of